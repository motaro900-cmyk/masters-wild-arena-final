import React from 'react';

/**
 * Эффекты перенесены в PixiJS (MainScreen.ts) для достижения 60 FPS.
 * Этот компонент теперь пустой, чтобы не создавать нагрузку на CPU.
 */
export const BackgroundEffects: React.FC = () => {
    return null; 
};
